# Relative to the user's home directory, e.g. $HOME
config: .config/pippim

# The event template used when creating a new event (relative to config)
template: event.template

# The location of the calendars
calendars: calendars

# The name of the user's personal calendar (no file extensions)
personal_cal: mine
